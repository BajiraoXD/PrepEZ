<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>Video call</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <!-- <link rel='stylesheet' type='text/css' media='screen' href='lobby.css'> -->
    <style>
         body {
            background-color: #1a1a1a;
            color: #fff;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            width: 100vw;
            background-image: url(../image/lobby_bg.jpg);
            background-size: cover;        
            background-position: center;   
            background-repeat: no-repeat;
        }

        #form-container {
            width: 90%;
            max-width: 600px;
            border-radius: 30px;
            background-color: rgba(38, 38, 37, 0.8); /* 80% opacity */
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            opacity: 0;
            transform: scale(0.9);
            animation: fadeIn 0.7s forwards, floating 3s ease-in-out infinite;
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        @keyframes floating {
            0% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
            100% {
                transform: translateY(0);
            }
        }

        #form__container__header {
            border-radius: 15px 0px 15px 0px;
            padding: 20px;
            text-align: center;
            background-color: rgba(54, 55, 57, 0.8); /* 80% opacity */
            margin: 0;
            font-size: 20px;
            font-weight: 300;
            animation: textFadeIn 1s forwards;
        }

        @keyframes textFadeIn {
            0% {
                opacity: 0;
                transform: translateY(-10px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        #form__content__wrapper {
            padding: 20px;
        }

        input[type="text"] {
            box-sizing: border-box;
            color: #fff;
            width: 100%;
            margin: 0;
            border: none;
            border-radius: 10px;
            padding: 16px 20px;
            font-size: 15px;
            background-color: #3f434a;
            margin-top: 32px;
            transition: background-color 0.3s;
        }

        input[type="text"]:focus {
            background-color: #454a50;
        }

        input[type="submit"] {
            background-color: #5625b8;
            color: #fff;
            border: none;
            padding: 16px 20px;
            border-radius: 10px;
            cursor: pointer;
            width: 100%;
            margin-top: 16px;
            transition: transform 0.2s;
        }

        input[type="submit"]:hover {
            background-color: #431f8a;
        }

        input[type="submit"]:active {
            transform: scale(0.95);
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            #form-container {
                width: 95%;
                padding: 15px;
                margin: 0 10px;
            }

            #form__container__header {
                font-size: 18px;
            }

            input[type="text"], input[type="submit"] {
                padding: 14px 18px;
                font-size: 14px;
            }

            body {
                flex-direction: column;
                justify-content: flex-start;
                padding-top: 20px;
            }
        }

        /* Add animation on button click */
        .button-clicked {
            animation: buttonClickedAnimation 0.3s ease-in-out;
        }

        @keyframes buttonClickedAnimation {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    
    <main id="lobby-container">
        <div id="form-container">
            <div id="form__container__header">
                <p>Join the Conversation: Create or join a Room 😊</p>
            </div>

            <div id="form__content__wrapper">
                <form id="join-form">
                    <input type="text" name="invite_link" placeholder="Enter invite ID" required/>
                    <input type="submit" value="Join Room" />
                </form>
            </div>
        </div>
    </main>



</body>

<script>
    let form = document.getElementById('join-form')

    form.addEventListener('submit', (e) => {
        e.preventDefault()
        let inviteCode = e.target.invite_link.value
        window.location = `vc?room=${inviteCode}` 
    })
</script>

</html>
